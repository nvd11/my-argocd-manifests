apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: astra-frontend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nvd11/my-shared-helm-charts.git
    path: charts/generic-web-service-v2
    targetRevision: main
    helm:
      values: |
        replicaCount: 1
        containerPort: 80

        image:
          repository: ghcr.io/nvd11/astra-frontend
          digest: sha256:0de4d31a4c7c5d0276d3c36211a5e698067dbde2c68ac84c33607e64ddb3972f
          pullPolicy: IfNotPresent

        livenessProbe:
          enabled: true
          path: /health
          port: http
          initialDelaySeconds: 10
          periodSeconds: 20
          failureThreshold: 3

        readinessProbe:
          enabled: true
          path: /health
          port: http
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3

        service:
          type: ClusterIP
          port: 80
          annotations:
            konghq.com/plugins: oauth2-forward-auth

        route:
          enabled: true
          parentGateway: kong-main-gateway
          path: /astra
          pathType: PathPrefix
          annotations:
            konghq.com/plugins: oauth2-forward-auth

        nodeSelector:
          kubernetes.io/hostname: free-arm-vm

  destination:
    name: tencent-dp1-cluster
    namespace: llm-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
