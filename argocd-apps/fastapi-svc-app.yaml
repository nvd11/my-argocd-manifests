apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fastapi-svc
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/nvd11/my-shared-helm-charts.git'
    path: charts/generic-web-service
    targetRevision: 'v1.1.2'
    
    helm:
      values: |
        replicaCount: 1
        containerPort: 8000
        
        image:
          repository: ghcr.io/nvd11/my-fastapi-svc
          tag: 9a269392c2fdfc76ed969d5c529edfc4550391b5
          pullPolicy: Always
          
        livenessProbe:
          path: /
          initialDelaySeconds: 15
          periodSeconds: 20
          
        readinessProbe:
          path: /
          initialDelaySeconds: 15
          periodSeconds: 20
          
        service:
          type: ClusterIP
          port: 80
          
        route:
          enabled: true
          parentGateway: kong-main-gateway
          path: /svc2
  destination:
    name: 'tencent-dp1-cluster'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
